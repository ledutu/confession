import {Container} from 'components';
import React from 'react';

const NotificationScreen = ({navigation}) => {
  return <Container title="Thông báo" canGoBack={false} isLineBottom />;
};

export default NotificationScreen;
