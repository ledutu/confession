const { User } = require('./user.model');
const { Answer } = require('./answer.model');
const { Category } = require('./category.model');
const { Question } = require('./questions.model');
const { RootQuestion } = require('./root-question.model');

module.exports = {
    User,
    Answer,
    Category,
    Question,
    RootQuestion,
}


